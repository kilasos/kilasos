# Installed by kilasos-nasd. nasd writes SSH keys added via the web UI to
# /etc/kilasos/authorized_keys (root-owned so sshd StrictModes accepts it).
# Listed alongside the per-user default so existing keys keep working.
AuthorizedKeysFile .ssh/authorized_keys /etc/kilasos/authorized_keys
